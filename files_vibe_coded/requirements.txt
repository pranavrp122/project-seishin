# Project Seishin — Phase 1 Python Dependencies

# Core async + HTTP
aiohttp>=3.9.0
websockets>=12.0

# Audio I/O
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.26.0

# vLLM (install separately with CUDA support)
# pip install vllm  →  handles Qwen inference
# See: https://docs.vllm.ai/en/latest/getting_started/installation.html

# (Optional) Silero VAD — drop-in upgrade over SimpleVAD
# pip install silero-vad torch torchaudio

# (Optional) Fish Speech — for local TTS server
# pip install fish-speech
# or: git clone https://github.com/fishaudio/fish-speech
