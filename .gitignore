.env
.env.cloud
__pycache__/
*.pyc
.DS_Store
*.log
.serena/

# Large audio dataset files — never commit
dataset_pipeline/audio/
dataset_pipeline/audio_shuffled/
dataset_pipeline/audio_wavs.tar.gz
dataset_pipeline/*.wav
.claude/

# Docker image tarballs (huge binary artifacts, never commit)
*.tar.gz
!dataset_pipeline/audio_wavs.tar.gz

# OAuth credentials and tokens — never commit
~/.sei/
*.token.json
gmail_token.json
gmail_credentials.json
client_secret_*.json
.venv/
