---
phase: 01-baseline-measurement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/tts_baseline/test_tags.py
  - /home/prana/tts-test/outputs/tag_tests/
  - /home/prana/tts-test/outputs/tag_tests/tag_report.md
autonomous: true
requirements: [BASE-04]
must_haves:
  truths:
    - "Each inline tag ([inhale], [slow], [fast], [pause], [low volume], [volume up], [emphasis], [short pause], [whisper]) has been tested with the current model"
    - "Tag responsiveness is documented with clear pass/fail per tag"
    - "Tag test recordings exist as WAV files for manual listening verification"
    - "All tag test clips uploaded to HuggingFace under humanism_baseline/tag_tests/"
  artifacts:
    - path: "tools/tts_baseline/test_tags.py"
      provides: "Tag testing script that generates with/without tag pairs and measures differences"
      min_lines: 100
    - path: "/home/prana/tts-test/outputs/tag_tests/"
      provides: "WAV pairs for each tag (with tag vs without tag)"
    - path: "/home/prana/tts-test/outputs/tag_tests/tag_report.md"
      provides: "Pass/fail results and measurements for each tag"
      contains: "PASS"
  key_links:
    - from: "tools/tts_baseline/test_tags.py"
      to: "http://127.0.0.1:8080/v1/tts"
      via: "POST with msgpack body, streaming WAV response"
      pattern: "requests\\.post.*msgpack"
---

<objective>
Test and document the effectiveness of all 9 Fish Speech S2-Pro inline tags ([inhale], [slow], [fast], [pause], [low volume], [volume up], [emphasis], [short pause], [whisper]) on the current model.

Purpose: Tag responsiveness gates future phases -- Phase 3 depends on [slow]/[fast]/[emphasis]/[short pause] working, Phase 4 depends on [inhale]/[low volume]/[volume up]/[whisper] working. If tags don't produce audible effects, those phases need alternative approaches. This must be known before planning executes.
Output: WAV file pairs (with/without each tag) for A/B listening, plus a structured pass/fail report documenting measurable effects. All clips uploaded to HuggingFace under humanism_baseline/tag_tests/.
</objective>

<execution_context>
@/home/prana/.claude/get-shit-done/workflows/execute-plan.md
@/home/prana/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@test_subchunk.py (reference for API call pattern, payload structure, WAV extraction)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tag testing script and generate A/B pairs</name>
  <files>tools/tts_baseline/test_tags.py</files>
  <read_first>
    - test_subchunk.py (API call pattern: ormsgpack payload, streaming response, WAV header skip at byte 44, int16-to-float32 conversion)
  </read_first>
  <action>
Create `tools/tts_baseline/test_tags.py` that tests each inline tag by generating paired recordings (with tag vs without tag on the same base text).

**Tags to test** (9 tags, each with a carrier sentence designed to reveal the tag's effect):

1. **[pause]**: 
   - With: "I need to tell you something. [pause] It's about what happened yesterday."
   - Without: "I need to tell you something. It's about what happened yesterday."
   - Measurement: Compare total audio duration (pause should add ~400-800ms)

2. **[inhale]**:
   - With: "[inhale] Ladies and gentlemen, I want to thank each and every one of you for being here tonight on this special occasion."
   - Without: "Ladies and gentlemen, I want to thank each and every one of you for being here tonight on this special occasion."
   - Measurement: Check first 500ms of audio for breath-like energy (non-silent audio before speech onset)

3. **[slow]**:
   - With: "The results are in and [slow] I regret to inform you that the project has been cancelled."
   - Without: "The results are in and I regret to inform you that the project has been cancelled."
   - Measurement: Compare total audio duration (slow section should be longer)

4. **[fast]**:
   - With: "So basically [fast] what happened was the server crashed and we lost everything and nobody knew what to do [fast] but then Sarah fixed it."
   - Without: "So basically what happened was the server crashed and we lost everything and nobody knew what to do but then Sarah fixed it."
   - Measurement: Compare total audio duration (fast sections should make it shorter)

5. **[short pause]**:
   - With: "Well [short pause] I suppose that makes sense when you think about it."
   - Without: "Well I suppose that makes sense when you think about it."
   - Measurement: Compare total audio duration (should add ~100-300ms, less than [pause])

6. **[emphasis]**:
   - With: "I told you [emphasis] not to touch that button under any circumstances."
   - Without: "I told you not to touch that button under any circumstances."
   - Measurement: Compare RMS energy of the emphasized segment (should be louder/more prominent)

7. **[low volume]**:
   - With: "Everyone can hear me fine but [low volume] this next part is just between us."
   - Without: "Everyone can hear me fine but this next part is just between us."
   - Measurement: Compare RMS of second half (low volume section should have lower RMS)

8. **[volume up]**:
   - With: "I started off quiet but then [volume up] I realized everyone needed to hear this part clearly."
   - Without: "I started off quiet but then I realized everyone needed to hear this part clearly."
   - Measurement: Compare RMS of second half (volume up section should have higher RMS)

9. **[whisper]**:
   - With: "[whisper] Don't look now but I think someone is watching us from across the room."
   - Without: "Don't look now but I think someone is watching us from across the room."
   - Measurement: Compare overall RMS and spectral characteristics (whisper should have lower RMS, more high-frequency noise)

**Also test combination**: "[inhale] I have an announcement. [pause] [slow] We are shutting down the program effective immediately."

**Script structure:**
1. Define tag test cases as a list of dicts: `{"tag": "pause", "with_text": "...", "without_text": "...", "metric": "duration_diff"}`
2. For each test case, generate both versions using the same API pattern as test_subchunk.py:
   - URL: `http://127.0.0.1:8080/v1/tts`
   - Payload (msgpack): `{"text": text, "reference_id": "archie", "format": "wav", "temperature": 0.7, "top_p": 0.8, "repetition_penalty": 1.2, "max_new_tokens": 1024, "chunk_length": 200, "streaming": True}`
   - Headers: `{"Content-Type": "application/msgpack", "Accept": "audio/wav"}`
3. **Generate each pair 3 times** (to account for model stochasticity) and report median values
4. Save WAVs to `/home/prana/tts-test/outputs/tag_tests/{tag}_with_run{N}.wav` and `{tag}_without_run{N}.wav`
5. Measure per pair:
   - `duration_with` and `duration_without` (seconds)
   - `duration_diff_pct` = (with - without) / without * 100
   - `rms_first_500ms` for [inhale] test (detect breath energy)
   - `rms_second_half` for [low volume], [volume up] tests
   - `rms_overall` for [whisper], [emphasis] tests
6. Apply pass/fail criteria:
   - [pause]: PASS if median duration_diff > 200ms
   - [inhale]: PASS if median rms of first 500ms is > 1.5x the without version
   - [slow]: PASS if median duration_with > 1.05x duration_without (5% longer)
   - [fast]: PASS if median duration_with < 0.95x duration_without (5% shorter)
   - [short pause]: PASS if median duration_diff > 50ms and < median [pause] duration_diff
   - [emphasis]: PASS if median RMS of emphasized segment is > 1.2x the without version
   - [low volume]: PASS if median RMS of second half is < 0.8x the without version
   - [volume up]: PASS if median RMS of second half is > 1.2x the without version
   - [whisper]: PASS if median overall RMS is < 0.5x the without version
7. Save results to `/home/prana/tts-test/outputs/tag_tests/tag_results.json`

Then run the script: `python tools/tts_baseline/test_tags.py`
  </action>
  <verify>
    <automated>python tools/tts_baseline/test_tags.py 2>&1 | tail -10 && python -c "
import os, json
td = '/home/prana/tts-test/outputs/tag_tests'
wavs = [f for f in os.listdir(td) if f.endswith('.wav')]
results = json.load(open(os.path.join(td, 'tag_results.json')))
tags_tested = [r['tag'] for r in results]
required = ['pause', 'inhale', 'slow', 'fast', 'short_pause', 'emphasis', 'low_volume', 'volume_up', 'whisper']
for t in required:
    assert t in tags_tested, f'{t} not tested'
print(f'OK: {len(wavs)} WAVs, {len(results)} tags tested: {tags_tested}')
for r in results:
    print(f\"  {r['tag']}: {r['result']}\")
"</automated>
  </verify>
  <acceptance_criteria>
    - `tools/tts_baseline/test_tags.py` exists with >= 100 lines
    - `/home/prana/tts-test/outputs/tag_tests/` contains WAV files for all 9 tags (with/without pairs, 3 runs each = 54+ WAVs)
    - `/home/prana/tts-test/outputs/tag_tests/tag_results.json` exists
    - tag_results.json contains entries for all 9 tags: pause, inhale, slow, fast, short_pause, emphasis, low_volume, volume_up, whisper
    - Each result entry has: tag, result (PASS/FAIL), median_duration_with, median_duration_without, duration_diff_pct
  </acceptance_criteria>
  <done>All 9 tags tested with 3 runs each. tag_results.json contains pass/fail for each. WAV pairs saved for manual A/B listening.</done>
</task>

<task type="auto">
  <name>Task 2: Generate tag responsiveness report</name>
  <files>/home/prana/tts-test/outputs/tag_tests/tag_report.md</files>
  <read_first>
    - /home/prana/tts-test/outputs/tag_tests/tag_results.json (measurement data from Task 1)
  </read_first>
  <action>
Add report generation to the end of `tools/tts_baseline/test_tags.py` (or run as a post-step). The script reads `tag_results.json` and writes `/home/prana/tts-test/outputs/tag_tests/tag_report.md`.

**Report format:**

```
# Inline Tag Responsiveness Report
Generated: {timestamp}
Model: Fish Speech S2-Pro (reference: archie)
Runs per tag: 3 (median reported)

## Summary

| Tag | Result | Duration With (s) | Duration Without (s) | Diff (%) | Threshold |
|-----|--------|-------------------|---------------------|----------|-----------|
| [pause] | PASS/FAIL | X.XX | X.XX | +XX.X% | >200ms diff |
| [inhale] | PASS/FAIL | X.XX | X.XX | +XX.X% | >1.5x RMS first 500ms |
| [slow] | PASS/FAIL | X.XX | X.XX | +XX.X% | >5% longer |
| [fast] | PASS/FAIL | X.XX | X.XX | -XX.X% | >5% shorter |
| [short pause] | PASS/FAIL | X.XX | X.XX | +XX.X% | >50ms diff, < [pause] diff |
| [emphasis] | PASS/FAIL | X.XX | X.XX | +XX.X% | >1.2x RMS of segment |
| [low volume] | PASS/FAIL | X.XX | X.XX | +XX.X% | <0.8x RMS second half |
| [volume up] | PASS/FAIL | X.XX | X.XX | +XX.X% | >1.2x RMS second half |
| [whisper] | PASS/FAIL | X.XX | X.XX | +XX.X% | <0.5x overall RMS |

## Detailed Results

### [pause]
- Carrier: "I need to tell you something. [pause] It's about what happened yesterday."
- Run 1: with=X.XXs, without=X.XXs, diff=X.XXs
- Run 2: ...
- Run 3: ...
- Median diff: X.XXs (X.XX%)
- Result: PASS/FAIL
- Implication for Phase 3: {if PASS: "[pause] tag viable for pause injection" | if FAIL: "Must rely on punctuation-only pauses"}

### [inhale]
...
- Implication for Phase 4: {if PASS: "[inhale] tag viable for breathing injection" | if FAIL: "Skip breathing injection, model handles natively from reference"}

### [slow]
...
- Implication for Phase 3: {if PASS: "[slow] tag viable for speech rate variation" | if FAIL: "Must rely on chunk-size manipulation for rate variation"}

### [fast]
...
- Implication for Phase 3: {if PASS: "[fast] tag viable for speech rate variation" | if FAIL: "Must rely on chunk-size manipulation for rate variation"}

### [short pause]
...
- Implication for Phase 3: {if PASS: "[short pause] viable for light hesitation injection" | if FAIL: "Use punctuation commas for light pauses"}

### [emphasis]
...
- Implication for Phase 3: {if PASS: "[emphasis] viable for stress on key words" | if FAIL: "Rely on model's natural prosodic stress"}

### [low volume]
...
- Implication for Phase 4: {if PASS: "[low volume] viable for dynamic volume control" | if FAIL: "Must use post-FX gain automation for quiet passages"}

### [volume up]
...
- Implication for Phase 4: {if PASS: "[volume up] viable for dynamic volume control" | if FAIL: "Must use post-FX gain automation for loud passages"}

### [whisper]
...
- Implication for Phase 4: {if PASS: "[whisper] viable for intimate/quiet speech style" | if FAIL: "Skip whisper effect or attempt via post-FX low-pass + gain reduction"}

## Combination Test
- Text: "[inhale] I have an announcement. [pause] [slow] We are shutting down the program effective immediately."
- Duration: X.XXs
- Observation: {brief note on whether tags compose well together}

## Implications for Subsequent Phases
- Phase 3 (Text Preprocessor): {summary of which rate/pause tags are usable}
- Phase 4 (Breathing): {summary of [inhale] viability}
```
  </action>
  <verify>
    <automated>python -c "
f = open('/home/prana/tts-test/outputs/tag_tests/tag_report.md').read()
for tag in ['pause', 'inhale', 'slow', 'fast', 'short pause', 'emphasis', 'low volume', 'volume up', 'whisper']:
    assert f'| [{tag}]' in f, f'Missing {tag} row'
assert 'Implications' in f, 'Missing implications section'
assert 'PASS' in f or 'FAIL' in f, 'Missing results'
print(f'OK: report is {len(f)} chars with all 9 tags and required sections')
"</automated>
  </verify>
  <acceptance_criteria>
    - `/home/prana/tts-test/outputs/tag_tests/tag_report.md` exists
    - Report contains summary table with all 9 tags
    - Each tag section has: carrier sentence, per-run measurements, median, result, implication
    - Report contains "Implications for Subsequent Phases" section referencing Phase 3 and Phase 4
    - Report contains either "PASS" or "FAIL" for each tag (no ambiguous results)
  </acceptance_criteria>
  <done>Tag report written with pass/fail for all 9 tags, per-run measurements, and implications for Phase 3 and Phase 4 planning.</done>
</task>

<task type="auto">
  <name>Task 3: Upload tag test clips to HuggingFace</name>
  <files>/home/prana/tts-test/outputs/tag_tests/</files>
  <read_first>
    - /home/prana/tts-test/outputs/tag_tests/tag_results.json (to confirm all clips exist)
  </read_first>
  <action>
Upload all tag test WAV files and the tag report to HuggingFace under a structured folder:

1. Use `huggingface-cli upload` to push the tag test outputs:
   ```
   huggingface-cli upload prana-seishin/project-seishin-data /home/prana/tts-test/outputs/tag_tests humanism_baseline/tag_tests --repo-type dataset
   ```
2. This uploads all WAV pairs (54+ files), tag_results.json, and tag_report.md into the `humanism_baseline/tag_tests/` folder on HuggingFace.
  </action>
  <verify>
    <automated>huggingface-cli repo files prana-seishin/project-seishin-data --repo-type dataset 2>&1 | grep "humanism_baseline/tag_tests" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - HuggingFace dataset repo contains humanism_baseline/tag_tests/ folder
    - All 54+ WAV files uploaded
    - tag_results.json and tag_report.md uploaded
  </acceptance_criteria>
  <done>All tag test clips and report uploaded to HuggingFace under humanism_baseline/tag_tests/.</done>
</task>

</tasks>

<verification>
1. `ls /home/prana/tts-test/outputs/tag_tests/*.wav | wc -l` >= 54 (9 tags x 2 versions x 3 runs)
2. `cat /home/prana/tts-test/outputs/tag_tests/tag_results.json | python -m json.tool` is valid JSON
3. `grep -c "PASS\|FAIL" /home/prana/tts-test/outputs/tag_tests/tag_report.md` >= 9
4. `grep "Implications" /home/prana/tts-test/outputs/tag_tests/tag_report.md` returns a match
</verification>

<success_criteria>
- All 9 inline tags tested: [pause], [inhale], [slow], [fast], [short pause], [emphasis], [low volume], [volume up], [whisper]
- 3 runs per tag for statistical robustness (median reported)
- Clear PASS/FAIL per tag with defined thresholds
- WAV pairs saved for manual A/B verification
- All tag test clips uploaded to HuggingFace under humanism_baseline/tag_tests/
- Report documents implications for Phase 3 (text preprocessor) and Phase 4 (breathing/volume)
- STATE.md blockers resolved with data: "[inhale] tag effectiveness unknown", "[slow]/[fast] tag effectiveness unknown"
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-measurement/01-02-SUMMARY.md`
</output>
